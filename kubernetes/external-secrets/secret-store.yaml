# 🎯 QUÉ HACE: Configura acceso a AWS Secrets Manager con IRSA
# 📍 CUÁNDO SE USA: Una vez, al instalar External Secrets Operator
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secrets-manager
  namespace: production
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:  # ✅ Usa IRSA (no necesita AWS keys en K8s)
          serviceAccountRef:
            name: external-secrets-sa
