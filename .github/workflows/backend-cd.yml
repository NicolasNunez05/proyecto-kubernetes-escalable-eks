name: Backend CD (Manual Deploy)

# 🎯 Trigger Manual: Solo se ejecuta cuando TÚ lo decides con un botón
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'production'
        type: choice
        options:
          - production

# 🌍 Variables de Entorno Globales (Coinciden con tu deployment.yaml)
env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: gpuchile-backend
  EKS_CLUSTER_NAME: gpuchile-cluster
  DEPLOYMENT_NAME: gpuchile-backend
  CONTAINER_NAME: backend
  NAMESPACE: default

jobs:
  deploy:
    name: Build & Deploy to EKS
    runs-on: ubuntu-latest
    
    steps:
      # 1️⃣ Bajar el código
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Configurar credenciales AWS (Usando tus secretos de GitHub)
      - name: 🔐 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 3️⃣ Login en Amazon ECR
      - name: 🔑 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 4️⃣ Construir y Subir Imagen Docker
      - name: 🏗️ Build, tag, and push Docker image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Importante: Entrar a la carpeta del backend
          cd apps/backend
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          # Subir a AWS ECR
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          # Guardar la URI para el siguiente paso
          echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      # 5️⃣ Conectar con Kubernetes (EKS)
      - name: ⚙️ Update kubeconfig for EKS
        run: |
          aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

      # 6️⃣ Desplegar (Actualizar imagen)
      - name: 🚀 Deploy to EKS
        run: |
          # Actualizamos solo la imagen del deployment existente
          kubectl set image deployment/${{ env.DEPLOYMENT_NAME }} \
            ${{ env.CONTAINER_NAME }}=${{ steps.build-image.outputs.IMAGE_URI }} \
            -n ${{ env.NAMESPACE }}

      # 7️⃣ Verificar éxito
      - name: ✅ Verify deployment status
        run: |
          kubectl rollout status deployment/${{ env.DEPLOYMENT_NAME }} -n ${{ env.NAMESPACE }} --timeout=180s